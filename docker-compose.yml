version: '3.8'

services:
    mysite:
        build: .
        command:
         python manage.py runserver 0.0.0.0:8000
        ports:
          - "8000:8000"
        volumes:
          - static_volume:/app/static
          - media_volume:/app/media
volumes:
    static_volume:
    media_volume:

